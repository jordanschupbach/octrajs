{
  "name": "octrajs",
  "version": "1.0.0",
  "main" : "index.js",
  "description": "JS bindings for octra",
  "author": "Jordan Schupbach",
  "license": "MIT",
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "devDependencies": {
    "node-gyp": "^10.0.1"
  },
  "scripts": {
    "generate": "node setup.js",
    "build": "npm run generate && node-gyp configure build",
    "test": "jest",
    "start": "node src/index"
  }
}
